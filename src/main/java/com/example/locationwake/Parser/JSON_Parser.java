package com.example.locationwake.Parser;

import com.example.locationwake.Database.mAttribute;
import com.example.locationwake.Database.mLocation;
import com.example.locationwake.Logger;

import org.json.JSONObject;

public class JSON_Parser {

    //TAG of the class
    static final private String TAG = "JSON_Parser";

    /**
     *
     * @param mAttribute
     * @return
     */
    public JSONObject JSON_Parser(mAttribute mAttribute) {
        Logger.logV(TAG, "JSON_Parser(mAttribute mAttribute): started with parsing the data to JSON");
        return null;
    }

    /**
     *
     * @param mLocation
     * @return
     */
    public JSONObject JSON_Parser(mLocation mLocation) {
        Logger.logV(TAG, "JSON_Parser(mLocation mLocation): started with parsing the data to JSON");
        return null;
    }

    /**
     *
     * @param mLocation
     * @param mAttribute
     * @return
     */
    public JSONObject JSON_Parser(mLocation mLocation, mAttribute mAttribute) {
        Logger.logV(TAG, "JSON_Parser(mLocation mLocation, mAttribute mAttribute): started with parsing the data to JSON");
        return null;
    }
}
